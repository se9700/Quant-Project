yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
PyYAML>=6.0
statsmodels>=0.14      # VAR dividend forecasting
lxml>=5.0              # required by yfinance get_earnings_dates (surprise feature)
torch>=2.0             # panel LSTM return forecaster (CPU wheel is fine:
                       # pip install torch --index-url https://download.pytorch.org/whl/cpu)
